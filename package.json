{
  "name": "dicoding-story-spa",
  "private": true,
  "version": "1.0.0",
  "description": "SPA (MVP) for Dicoding Story API with camera + map + accessibility + View Transition API",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:prod": "vite preview --port 4173",
    "deploy": "node deploy.js && npx gh-pages -d dist"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "vite": "^7.1.3",
    "vite-plugin-pwa": "^1.0.3"
  },
  "dependencies": {
    "firebase": "^12.1.0",
    "leaflet": "^1.9.4"
  }
}